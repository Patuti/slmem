#include "slmem.h"

// this file only exists to be able to create a cmake static lib
// which shouldn't be linked by any other project, but will at least
// be visible in the generated IDE files (only tested with Visual Studio 2015)